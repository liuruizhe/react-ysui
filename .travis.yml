language: node_js
node_js:
  - stable
cache:
  directories:
    - node_modules
script:
  - npm test
  - npm run coverage
before_install:
    - npm install --save react-dom@15.3 react-router@4 ysui
after_script:
    - npm install coveralls && nyc report --reporter=text-lcov | coveralls
